# FakeNewsDetection-NLP
Built a complete fake-news classifier using TF-IDF models (SVM, Logistic Regression) and deep learning models (CNN, BiLSTM, MLP) with GloVe, Word2Vec, and USE embeddings. Also fine-tuned BERT for contextual accuracy. Includes preprocessing, model training, evaluation, and performance comparison.
